Stack: Launch -> Current Weather Page -> Locations Page

Init:
    - setup single object with injection
    - create global state for `City Controller`
        - current city (integration from cache)
        - cities (integration from cache)
    - create global state for `Current Weather Bloc`
    - create global state for `Hourly Weather Bloc`
    - create global state for `Locations Bloc`

Get Current Weather Page:
    - initialize `City Controller`
    - refres(): if currentCity is present
        - call `OnGetCurrentWeather` event
        - call `OnGetHourlyWeather` event
    - show response to BlocBuilder

Locations Page:
    - init (refresh):
        // if state are present, return
        // if state empty, call
    - Add New City:
        - show modal bottom sheet
        - submit:
            - update cities in Controller and cache
            - refresh list
    - when click item:
        - set current city
        - back to current weather page and refresh